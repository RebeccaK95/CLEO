 <div class="column flex-column d-flex justify-content-between">
    <h3><%= @challenge.name %></h3>
    <p><%= @challenge.category.name %></p>
    <p><%= @challenge.description %></p>
    <p><%= @challenge.steps %></p>
    <div class="link-show">
      <i class="fas fa-hand-point-left"></i><%= link_to "  Go back to all challenges", challenges_path  %>
    </div>
  </div>
<%= link_to "Start challenge", challenge_user_challenges_path(@challenge), method: :post %>
<%= link_to "Mark as completed", challenge_user_challenges_path(@challenge), method: :patch %>
